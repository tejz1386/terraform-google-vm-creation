# terraform-google-vm-creation
This module is used to create a Virtual Machine in GCP.
This uses input from a csv file and depends upon a separate module that collects the values from the csv file.
-----------------------------------------------------------------------------------